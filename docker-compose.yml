services:
  faq-assistant:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      # Persist both Chroma vectors and SQLite leads outside the image lifecycle.
      - ./data:/app/data
    restart: unless-stopped
